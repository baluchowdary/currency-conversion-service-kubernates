# currency-conversion-service-kubernates

$ git init -b master

$ git add .

$ git commit -m "First commit"

$ git remote add origin  https://github.com/baluchowdary/currency-conversion-service-kubernates.git

$ git remote -v

$ git push origin master
